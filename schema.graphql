type Query {
	getProducts: [Product]
	productById(product_id: ID!): Product
}

type Product {
	product_id: ID!
	product_name: String
	style: String
	category: String
	imageUrl: String
	description: String
}
