type Query {
	getProducts: [Product]
	productById(productId: ID!): Product
}

type Product {
	productId: ID!
	productName: String
	style: String
	category: String
	imageUrl: String
	description: String
}
